# Pinned to what every number in the README was produced with.
# Python 3.10 or newer (3.12 used for the results here).

mujoco==3.11.0
gymnasium==1.3.0
torch==2.13.0          # CPU build is enough; see docs for the GPU note
numpy>=1.26,<3
matplotlib>=3.8,<4
imageio>=2.34,<3
imageio-ffmpeg>=0.4.9  # MP4 writing only; GIFs need just imageio

# Development
pytest>=8.0
flake8>=7.0
