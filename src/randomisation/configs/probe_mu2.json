{
  "name": "probe_mu2",
  "scale": 1.0,
  "params": {
    "object_friction": {
      "low": 2.0,
      "high": 2.0,
      "mode": "scale"
    }
  },
  "_comment": [
    "Probe distribution: object friction pinned at 2x nominal, everything",
    "else at its nominal value. Used to find where cross-simulator transfer",
    "saturates in friction."
  ]
}
